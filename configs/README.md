# Configuration Map

The stable configuration directory is `config/`. It is intentionally not renamed because runners and historical commands resolve that path. Experiment metadata and `docs/indexes/EXPERIMENT_INDEX.md` map individual configs to BATON IDs.
