# Trace data

This directory is reserved for trace schemas and deliberately small, curated examples used by route-observability tests. Raw traces and runtime capture trees must be externalized and referenced through `data/manifests/`.
