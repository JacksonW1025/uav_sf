# Follow-up experiment readiness

## Verified entry

The complete Thor container and formal campaign entry can directly run a newly
preregistered Family A official-sequence matrix that uses existing live fixture
semantics, or a three-strategy matrix using the qualified owned setpoint-stall
backend. Verification across the closed studies covered:

- exact local image identity and ARM64 container preflight;
- repository, dependency, binary, environment, method, and safety digests;
- empty/pending dry-run scheduling and completed-ledger no-op resume checks;
- 295 closed formal launches with no open attempt, reported under separate
  study denominators;
- four-slot isolation and two-active-attempt supplemental execution;
- a live-runtime/processing barrier that keeps ULog and Oracle work out of the
  timing-sensitive SITL phase;
- compact-result digest verification and reproducible study summaries;
- host validation of all tracked files and unit tests.

The invocation remains:

```bash
python3 -m scripts.runtime.run_campaign \
  --matrix PATH/TO/NEW/matrix.json \
  --attestation PATH/TO/NEW/environment-attestation.json \
  --study-root PATH/TO/NEW \
  --run-root runs \
  --image EXACT_ATTESTED_IMAGE
```

The formal default remains concurrency four. Five-way is retained only as a
non-formal qualification result; it must not be selected by a formal matrix.
See `experiments/concurrency_barrier_qualification/` for the frozen specs and
decision record.

## Completed Stage A2 boundary

The V7 moving-workload realism bridge is implemented and complete. Its shared
position-only straight-line workload, motion-phase observations, physical
validity contract, observer and safety qualification, frozen identities, and
separate formal matrices are retained with the Stage A2 studies. The primary
ledger remains insufficient; the independent remediation reached all four
targets with 26/26 accepted and admissible launches. This closes Motivation
Study execution but does not authorize a strategy comparison under the Stage
A2 identity.

## Completed Section 7 vertical slice

The separate `main-strategy-comparison-thor-v1` identity closes exactly 18
formal launches across two mechanisms and three strategies. All attempts are
accepted, admissible, physically valid, and ledger-closed. Its result qualifies
the one-action backend and reports bounded timing coverage; it does not turn
the Stage A2 denominator into a strategy study or complete the full Main
Evaluation.

## Explicit limitation

`bounded_random_timing` and `state_aware` now have a qualified live backend for
the owned moving setpoint-stall action. Six non-formal qualification flights and
18 separately preregistered formal launches demonstrate decision, schedule,
live-state gating, action request, observed fault, and evidence closure. The
formal result is complete with bounded claims.

This readiness does not extend automatically to other public commands, faults,
setpoint semantics, routes, or vehicles. Those combinations still require an
explicit backend contract and fail closed. The 18-launch sample also does not
rank random versus state-aware: each covered three timing bins and exposed the
same freshness signature. The next experiment must broaden the action/route
corpus and include confirmation and ablation before claiming general search
effectiveness.
