# Goal recovery state

The 2026-07-17 Goal session was interrupted during `campaign_seeded_v3`. The
repository, process tree, and ignored campaign artifacts were audited before
any new SITL process was launched.

## Recovered roles

- `campaign_seeded_v2` is preserved as
  `PRESERVED_EXCLUDED_REVISION_MISMATCH`. It used an earlier Dynamic adapter
  and observation/capture revision and is excluded from all v3 analysis.
- `campaign_seeded_v3` is the recovered campaign. It is now
  `CLOSED_REVISION_CHANGE_REQUIRED`, not restarted or renamed merely because
  the Goal resumed.
- The two dirty tracked P5 analysis products were v2-derived final outputs,
  not v3 results. Their interrupted versions were copied into ignored recovery
  evidence before the tracked files were restored to the last committed
  checkpoint. The recovered SHA-256 values are
  `e1b85886dc56d736890c7d29f11aba35e3bddb19cf490aea4e27dcf9f1d68a65`
  (`paired_results.tsv`) and
  `7547711e224da5fd0a8ce2616e75b1e72f8c571b79d370a918aa7405421ec83d`
  (`summary.json`).
- The untracked Fuzzer v0 files predated recovery. They form a unit-tested core
  implementation snapshot, but are not Fuzzer Readiness evidence and are not
  authorized for campaign execution while P5 is incomplete. They are being
  committed only to prevent the interrupted work from being lost.

## v3 terminal finding

The strengthened Dynamic T5 pilot has valid behavioral evidence: Hold was both
expected and observed, the clock bridge is valid, the selected critical window
is complete, source and target epochs are present, post-revocation old-epoch
consumption and writer counts are zero, and all five Oracle clauses pass.
However, its embedded observation provenance records patch `4ac3b87e...`, not
v3's canonical `73555576...`; it is therefore preserved but excluded from
campaign acceptance under the strict identity rule.

The recovered T1 evidence contains two clean reproductions in which Dynamic
External Mode completed successfully but remained nav state 23 until the
runner timed out requesting Hold. The registered mode replaces internal Hold,
so internal Hold cannot be selected until that component unregisters. This is
a campaign-blocking adapter lifecycle defect, not a SUT or Oracle violation.
Changing that lifecycle is a genuine adapter revision and is the reason v3 is
closed. A new campaign version is permitted only after the fix is committed,
built, and validated by a matching pilot.

## Recovered execution state

- accepted sides: 2 of 70 (T1 r1/r2 Offboard)
- complete pairs: 0 of 35
- affected pairs: T1 r1 invalid due campaign configuration; T1 r2 partial
- untouched pending pairs: 33
- confirmed environment failures: 3 PX4 abort/incomplete attempts
- excluded measurement attempts: 2 invalid clock bridges
- active campaign or simulator processes: none

The machine-readable reconstruction is generated by
`scripts/probes/p5_campaign_manifest.py`. Raw evidence remains ignored under
`runs/`; no attempt was overwritten or deleted.
