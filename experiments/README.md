# Experiment Metadata

This tree is the canonical BATON classification layer. To preserve runnable scripts, historical links, and raw-data provenance, most artifacts remain at their stable legacy paths and are referenced by `experiment.yaml` and `docs/indexes/EXPERIMENT_INDEX.md`.

Missing fields are `unknown`; legacy results are retained rather than rewritten. The metadata files do not modify result values.
