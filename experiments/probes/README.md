# Motivation probes

This directory contains focused probes that follow route-observability
feasibility. A probe must identify the route fields it measures, timestamp
domains, expected handoff, and external raw-output location. The only completed
probe in this phase is the gated [P0 normal-flow baseline](p0/README.md). Do not
add a large campaign runner here.
