# Active PX4 patches

Only observation-only, exact-commit PX4 patches required by the Family A route
model belong here. Each patch must be idempotently checkable and have an
automated validation test. Optional learned-controller patches are isolated in
`family_b/patches/`.
