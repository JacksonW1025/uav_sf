# PX4 board overlays

The active board overlays retain the minimum mc_nn and RAPTOR Family B build paths. Install them with the scripts in `scripts/setup/`; do not edit the ignored PX4 clone as the sole source of a change.

The pre-V4 unclipped-ablation board is archived under `archive/pre_v4_baton/configs/boards/`.
