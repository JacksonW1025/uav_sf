# Artifact Layout

Canonical categories are reports, tables, figures, and logs. Existing artifacts remain at stable `docs/`, `img/`, `runs/`, and campaign paths; the artifact manifest provides the canonical lookup.
